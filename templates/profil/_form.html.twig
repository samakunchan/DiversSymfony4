{{ form_start(form, { 'attr':{'id': 'profil_form'}}) }}
    {{ form_widget(form) }}
    <img src="" alt="" id="avatar">
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

